# Isometric_MTO
A full-stack web application where a user can upload one piping isometric drawing (image or PDF) and the system automatically generates a Material Take-Off (MTO) using an AI pipeline on the backend.
